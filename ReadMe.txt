O nosso WebServer esta na root do projecto. O codigo do Maze Runner encontra-se na pasta src/main/java. 

Na pasta DB temos a classe para escrever as metricas na bse de dados. 

Na pasta LoadBalancer temos toda a logica do LoadBalancer e do Auto-Scaler. A classe LoadBalancer faz o trabalho de load balancer e auto scaler.  


javac $(find . -name "*.java")

